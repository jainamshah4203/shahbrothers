"use client";

import React from "react";
import Hero3D from "@/components/Hero3D/Hero3D";

/**
 * Homepage hero — fullscreen scroll-driven Hero3D desk scene.
 */
export const Hero: React.FC = () => {
  return <Hero3D />;
};

export default Hero;
