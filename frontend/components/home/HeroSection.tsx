import Hero3D from "@/components/Hero3D/Hero3D";

/**
 * The main Hero section for the homepage.
 * Wraps the interactive 3D hero experience.
 */
export default function HeroSection() {
  return <Hero3D />;
}
